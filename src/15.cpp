#include <iostream>
using namespace std;

int main() {
    int height = 5;
    int width = 3;
    
    if (height > width) {
        cout << "Height is greater than width." << endl;
    } else {
        cout << "Height is less than or equal to width." << endl;
    }
    
    return 0;
}
